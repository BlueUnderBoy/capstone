
<%= form_with(model: goal, class: "card-body") do |form| %>
  <% if goal.errors.any? %>
    <div id="error_explanation">
      <ul class="list-unstyled">
        <% goal.errors.each do |error| %>
          <li>
            <div class="alert alert-danger" role="alert">
              <%= error.full_message %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :image %>
    <%= form.file_field :image, class: "form-control" %>
    <%= form.hidden_field :owner_id, value: current_user.id, class: "form-control" %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :amount_needed, style: "display: block" %>
    <%= form.text_field :amount_needed %>
  </div>

  <div>
    <%= form.label :amount_saved, style: "display: block" %>
    <%= form.text_field :amount_saved %>
  </div>

  <div>
  <%= form.hidden_field :status, value: "saving" %>
</div>

  <div class="actions d-grid gap-2">
    <%= form.submit class: "btn btn-outline-primary btn-block" %>
  </div>
<% end %>
